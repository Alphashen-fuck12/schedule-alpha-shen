---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 10ff82c76385fb81fe56f4a0b12d2d68_f1dead99a83e11f18567525400aeaaa3
    ReservedCode1: 9iQznX+xNP3aATMOEnNNdkyogfF8c42X1cTOVjlGxQnW+GhlgRIa/jbk4eYGb/RU0vT/4sbrFJzZ3Su4eC0XpuGwqShv9yfFH9d1jOFMpt9rC2TexXYf4wtigQmCrli6VqGMxN1eC6s9ZjAZGTYkqonpTriTFys4gJXjoXDyAKwI7Men6vGdxsCtO8E=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 10ff82c76385fb81fe56f4a0b12d2d68_f1dead99a83e11f18567525400aeaaa3
    ReservedCode2: 9iQznX+xNP3aATMOEnNNdkyogfF8c42X1cTOVjlGxQnW+GhlgRIa/jbk4eYGb/RU0vT/4sbrFJzZ3Su4eC0XpuGwqShv9yfFH9d1jOFMpt9rC2TexXYf4wtigQmCrli6VqGMxN1eC6s9ZjAZGTYkqonpTriTFys4gJXjoXDyAKwI7Men6vGdxsCtO8E=
---

# 电子记课表（课表应用）项目交接文档

> 版本：v1.3.0 ｜ 交接日期：2026-09-05 ｜ 项目根目录：`G:\周课表`
> 本文档面向后续接手维护/部署本项目的任何人，涵盖项目背景、目录结构、软件信息、核心逻辑、构建发布方式与注意事项。

---

## 一、项目概述

- **产品**：电子记课表（桌面应用 + 浏览器双形态），是给 7 位初中老师定制的桌面礼物。
- **用途**：老师日常排课、换课、备忘、每日祝福、导出课表/打印。
- **核心诉求**：
  - 界面**只显示老师姓氏**，不显示全名；
  - **不出现"教师节"字样**，主打每日祝福（约 1/15 概率夹带"爱徒沈涵方"彩蛋）；
  - 交付给老师**不携带任何个人使用数据**，首次打开即空白状态；
  - 离线优先，校时一律用系统时间，不消耗老师网费。
- **技术栈**：Tauri v2 + Rust + 原生 Web 前端（无框架，单文件 HTML）。
- **运行环境**：桌面版仅 Win10/11；浏览器版兼容老系统（Win7 及以上现代浏览器）。

---

## 二、工作区与目录结构（G:\周课表）

```
G:\周课表\
├── schedule.exe                     # v1.2.0 便携主程序（双击即用，免安装）
├── index.html                       # 浏览器版（老电脑兜底，零依赖）
├── 使用教程.md                       # 图文操作说明（交付给老师）
├── 数据迁移说明.md                   # 换电脑/备份迁移指引（交付给老师）
├── 课表应用_1.2.0_交付包.zip          # 交付压缩包（4件套，约3.9MB）
├── version.json                     # 自动更新清单（版本/下载URL/SHA256）
├── AGENT.md                         # 项目协作规则：每次改动 commit+测试
├── .gitignore                       # 忽略构建产物/历史版本/交付包/归档
├── _项目档案\                        # 项目归档（已 gitignore，不入库）
│   ├── 交付与报告\                   # 6份审查/审计/规划报告 md + 11张截图 + 备份
│   │   └── backup_v1.2.0_20260903\  # 全量源码备份（62MB/41文件）
│   └── 构建过程\                     # 构建日志、代码检查脚本、截图分析脚本
├── src-tauri\                       # Tauri v2 Rust 工程
│   ├── src\main.rs                  # Rust 主逻辑（窗口/更新/导出/自动启动）
│   ├── frontend\                    # 前端资源（打包进 exe 的源）
│   │   ├── index.html               # 主界面（须与根目录 index.html 保持同步！）
│   │   └── widget.html              # 桌面小组件界面
│   ├── tauri.conf.json              # Tauri 配置（见下）
│   ├── Cargo.toml                   # Rust 依赖清单
│   ├── icons\                       # 应用图标
│   └── target\                      # 构建缓存（已 gitignore）
└── dist\                            # 构建输出目录
    ├── schedule.exe                 # 最新可执行文件（与根目录同文件）
    ├── 课表应用_1.2.0_x64-setup.exe  # NSIS 安装器（一键安装+支持自动更新）
    └── 使用说明.html                 # 安装器配套说明页
```

**注意**：`dist/`、`src-tauri/target/`、`_项目档案/`、`课表应用_*_交付包.zip` 及历史版本产物均已写入 `.gitignore`，不会误提交。

---

## 三、最新软件全部信息（v1.2.0）

### 3.1 版本与产物

| 项 | 值 |
|---|---|
| 版本号 | 1.2.0 |
| 产品名 / 标识 | 课表应用 / `com.teacher.schedule` |
| 便携版 exe | `schedule.exe`，12,518,912 字节 |
| 便携版 SHA256 | `66E71F9BEC959A5B6F940FFBD414283376442339D357193D95A857D29108662B` |
| NSIS 安装器 | `课表应用_1.2.0_x64-setup.exe`，3,013,884 字节 |
| 交付包 | `课表应用_1.2.0_交付包.zip`，约 3.9MB（schedule.exe + index.html + 教程 + 迁移说明） |

### 3.2 技术配置

- **Tauri 配置**（`src-tauri\tauri.conf.json`）：`frontendDist = frontend`、`withGlobalTauri = true`、`bundle.targets = ["nsis"]`、图标 `icons/icon.ico`、`csp = null`。
- **Rust 依赖**（`src-tauri\Cargo.toml`）：
  - `tauri v2`（启用 `tray-icon`）
  - `tauri-plugin-autostart`（开机自启）
  - `tauri-plugin-notification`（系统通知）
  - `reqwest 0.12`（blocking + rustls，自动更新下载，不依赖系统 CA 问题）
  - `sha2 0.10`（SHA256 校验）
  - `rfd 0.15`（系统保存/打开对话框）
  - `dirs 6`、`serde`、`serde_json`
  - Windows 专用：`windows 0.61`（Win32 窗口置底 API）

### 3.3 运行依赖（已验证）

- exe 导入表仅依赖 Windows 系统 DLL（KERNEL32/user32/shell32/gdi32 等）+ UCRT（Win10/11 内置）。
- **无需安装 VC++ Redistributable、.NET**（Rust 静态链接）。
- 唯一外部依赖：**WebView2 Runtime**（Win10/11 系统更新自带，本机已装 151.0.4129.107）；缺失时用浏览器版 `index.html` 兜底。

---

## 四、功能清单（v1.2.0 最终形态）

1. **双形态打开**：桌面 exe（推荐）/ 浏览器 index.html，功能完全一致。
2. **首页**（v1.2.0 新增）：日历视图（今天高亮、有课日期带圆点、点日期跳转对应周）、今日课表概览、每日祝福直达、快捷按钮；可切换「首页/课表视图」并记忆。
3. **学期管理**（v1.2.0 新增）：切换学期、新建学期（自动沿用上一学期节次/班级/年级生成空框架），数据互不干扰。
4. **课表网格编辑**：点空格添加课程条目（课程/年级/班级/门牌号/备注），一格可多条，可改可删。
5. **智能换课**：源/目标格 对调 / 移动 / 改课 / 清空，历史记录可逐步撤销。
6. **每日祝福**：识别老师姓氏与学科，每日弹祝福卡，约 1/15 概率夹带"爱徒沈涵方"。
7. **简洁/玻璃双模式**：玻璃模式（半透明+5套主题+背景图，默认）与简洁模式（白底黑线大字号），偏好记忆。
8. **导出/导入**：导出 Excel（`课表_老师名_日期.csv`，UTF-8 BOM 中文不乱码）、导出/导入备份（JSON），含导入校验。
9. **打印**：玻璃模式打印自动转白底黑字版式，打印字号 ≥14px。
10. **备忘录**：自动保存。
11. **桌面小组件**：无边框透明 320x420 只读小窗，置底不抢焦点，托盘常驻，可选开机自启（`--widget` 模式）。
12. **自动更新**：后台静默检查 + 手动"检查更新"，SHA256 校验后备份替换，数据不丢。

---

## 五、核心逻辑与机制

### 5.1 数据模型与存储

- 数据存于 WebView `localStorage`，键名 **`STORAGE_KEY = "elec_schedule_full_v1"`**。
- 学期、班级、年级、课程、课表格子、预览周覆盖（`weekOverrides`）、提醒、界面偏好等均在其中。
- 数据不随 exe 替换丢失（exe 只替换程序，localStorage 在 WebView 用户数据目录）。
- 换电脑需走「导出备份/导入备份」。

### 5.2 双窗口结构（单 exe）

- **主窗口**：完整编辑界面（Tauri `WebviewWindowBuilder` 构建）。
- **小组件窗**：只读无边框透明 320x420 小窗；Windows 下取 HWND 用 `user32 SetWindowPos(HWND_BOTTOM)+SWP_NOMOVE|SWP_NOACTIVATE` 置底、不抢焦点。
- **数据同步**：主窗数据变更时 `app.handle().listen_any('schedule:changed')` → `emit` 给小组件；前端用 `window.__TAURI__.event.listen('schedule:changed')` 接收刷新。
- 小组件关闭事件 `prevent_close` 改为 hide，常驻托盘。

### 5.3 自动更新链路

```
version.json(远程) ──fetch──▶ main.rs 解析版本
   ├─ 版本低/手动检查 ─▶ 下载远程 schedule.exe ─▶ SHA256 校验
   │                      （不匹配则中止）
   └─ 匹配 ─▶ apply_update：批处理 move 覆盖 current_exe，
               旧版备份 schedule_old.exe（超60天清理）
```

- 更新清单地址：`https://raw.githubusercontent.com/Alphashen-fuck12/schedule-alpha-shen/master/version.json`
  （`main.rs` 中 `DEFAULT_UPDATE_URL` 常量，可用环境变量 `SCHEDULE_UPDATE_URL` 覆盖以便测试）。
- 更新暂存目录：`%APPDATA%\schedule\update\`。
- **容错设计**：拉取失败/超时/仓库不可达时，后台检查与"检查更新"均**静默失败**，不影响课表正常使用。

### 5.4 其他机制

- **开机自启**：`tauri-plugin-autostart`，自启参数 `--widget`（只显小组件收托盘）。
- **导出 Excel**：exe 形态用 Rust 命令 `save_text_file`（rfd 保存对话框 + `std::fs` 写盘）；浏览器版直接下载。
- **兼容性加固**：老内核浏览器 14 处 `querySelectorAll(...).forEach` 已改为 `[].forEach.call` 并补 polyfill。
- **提醒去重**：`remindOnce=false` 时按分钟去重，避免提醒轰炸。

---

## 六、关键版本历史与修复（维护必读）

### 6.1 版本线

- **v1.1.1**：年级/班级支持自定义。
- **v1.1.2**：资源优化——小组件默认关闭、简易模式默认、ticker 降频 15s、事件驱动渲染。
- **v1.2.0**：新增首页/学期延续/NSIS 安装器 + 全量修复 + 自动更新就绪。
- **v1.3.0（当前）**：界面重构——顶部杂牌按钮全部收敛为左侧分组式交互侧边栏，智能换课升级为独立整页视图，顶栏仅保留标题与周徽章；前端双副本已同步，浏览器版验收通过（三视图正常、0 JS 报错），待构建 exe。

### 6.2 v1.2.0 关键修复（源自代码审查报告 S/M 级）

**严重（S1–S6）**
- S1 `saveSetup` 无重名校验 → 学期覆盖 + 引用串档。
- S2 预览周清空/删除/对调空格 = delete override 恢复默认模板。
- S3 预览周删"沿用默认"格 splice 改默认模板引用 → 毁默认课表。
- S4 `remindOnce=false` 提醒轰炸。
- S5 `detectBlessProfile` 学科唯一误匹配（如"数学"错配吴建勇）+ 每次启动必弹 + 1/15 夹带逻辑。
- S6 `importBackup` 无学期迁移。

**重要（M1–M14 摘录）**
- M1/M2/M3 首页/CSV/小组件不读 `weekOverrides`（预览周数据）。
- M4 小组件与主程序双重提醒。
- M5 取消保存仍触发浏览器下载。
- M6 换课写入空课程。
- M7 主窗口关闭即退出、无托盘。
- M9 Program Files 目录更新失败。
- M10 `DEFAULT_UPDATE_URL` 曾为占位符（已改真实仓库地址）。
- M11 开机自启曾误带 `--widget` 模式。
- M12 智能换课按钮曾误返课表。

### 6.3 兼容性（老师端根因）

- v1.1.2 交付时老师端"添加条目无效"根因：发送的压缩包不含数据，老师解压用 exe 即全新空课表；空数据下点空白格添加失败的**真实缺陷**（`cellFor` 空格返回临时数组 push 丢失不写回）。
- v1.2.0 已改为 `concat + setCell` 写回，空白格添加成功。

---

## 七、制作 / 构建 / 打包方式

### 7.1 关键纪律

1. **前端双副本同步**：修改 `index.html` 必须**同时改两份**——根目录 `index.html` 与 `src-tauri\frontend\index.html`，否则打包不生效。两份 MD5 校验一致（当前 = `10D44C570C8C2E2EC185D2E900BF46B3`）。
2. `widget.html` 仅 `src-tauri\frontend\widget.html` 有副本（MD5 `F30A81D0...`）。
3. 每次改动按 `AGENT.md` 要求 commit + 测试。

### 7.2 构建步骤（Windows / PowerShell）

```powershell
cd G:\周课表
# 1) 同步前端到 src-tauri\frontend 后，构建 release exe
cargo build --release --manifest-path src-tauri\Cargo.toml
#    产物：src-tauri\target\release\schedule.exe（Rust 编译约 1m29s）

# 2) 复制到根目录与 dist（保持三处一致：target/release、根、dist）
Copy-Item src-tauri\target\release\schedule.exe schedule.exe -Force
Copy-Item src-tauri\target\release\schedule.exe dist\schedule.exe -Force

# 3) 打 NSIS 安装器
cargo tauri build --bundles nsis --manifest-path src-tauri\Cargo.toml
#    产物：src-tauri\target\release\bundle\nsis\课表应用_1.2.0_x64-setup.exe（约48s）
#    复制到 dist\
```

> 经验：`cargo tauri build` 单独跑可能日志丢失不出 exe，建议先 `cargo build --release` 再 `cargo tauri build --bundles nsis`。
> 构建用 `Start-Process` 后台重定向日志，避免 shell 截断/超时卡死。

### 7.3 哈希与自动更新清单

```powershell
# 计算新 exe 的 SHA256，写入 version.json
(Get-FileHash schedule.exe -Algorithm SHA256).Hash
```

- `version.json` 结构：`{"version":"1.2.0","url":"<raw链接>/dist/schedule.exe","hash":"<SHA256>"}`。
- 前端 assets 以 gzip/压缩嵌入 exe，直接在 exe 里搜字符串搜不到，需解压验证。

### 7.4 冒烟测试

- 启动 exe 后确认进程存活（`Get-Process schedule`），再人工验证：版本号显示、添加条目、导出对话框、小组件开关、首页、学期切换。

### 7.5 发布推送（GitHub）

```powershell
cd G:\周课表
git add <改动的源文件>          # 只提交源码/配置，交付包与归档已 gitignore
git commit -m "描述"
git push origin master
```

- 仓库：`https://github.com/Alphashen-fuck12/schedule-alpha-shen`（master），Git 用户 Marvis。
- 当前已推送最新提交 `c67010f`，远程与本地同步。
- **自动更新发布物**：`dist/schedule.exe` + `version.json` 必须随源码一起推送。

---

## 八、交付与使用规范

### 8.1 交付给老师的内容

仅发 **4 件套**（已打包为 `课表应用_1.2.0_交付包.zip`）：
1. `schedule.exe`（主程序）
2. `index.html`（浏览器版兜底）
3. `使用教程.md`
4. `数据迁移说明.md`

> 交付包不含安装器（用户明确不要）、不含任何个人课表数据，首次打开即空白。

### 8.2 界面内容红线

- 只显姓氏、不显全名；
- 不出现"教师节"字样，主打每日祝福（约 1/15 概率夹带"爱徒沈涵方"）；
- 校时用系统时间，不联网消耗老师流量。

---

## 九、待办与注意事项

1. **自动更新网络受限（重要）**：当前网络下 `raw.githubusercontent.com` 被阻断（github.com / api.github.com 可达），运行时"检查更新"会**静默失败**。机制与资源已就绪（version.json 哈希自洽、远程 exe 与本地一致），但要真正可用需：换可访问 raw 的网络 / 开代理，或将 `version.json` 的 url 与 `main.rs` 的 `DEFAULT_UPDATE_URL` 改为国内加速前缀（如 gh-proxy.com 等）。
2. **发布前检查**：M10 相关——确认发布仓库真实存在且 `version.json`/`dist/schedule.exe` 已放置（本轮已推送验证）。
3. **老师端真实验收**：v1.2.0 全量修复需实际发到老师电脑验证（当前本机冒烟测试通过）。
4. **数据备份**：重大改动前按 `_项目档案\交付与报告\backup_v1.2.0_20260903` 的方式先做全量备份。

---

## 十、常用参考

- **使用教程**：`G:\周课表\使用教程.md`
- **数据迁移说明**：`G:\周课表\数据迁移说明.md`
- **代码审查报告**：`G:\周课表\_项目档案\交付与报告\v1.2.0_代码审查报告.md`
- **浏览器兼容性审计**：`G:\周课表\_项目档案\交付与报告\周课表浏览器兼容性审计报告.md`
- **老师端问题根因审查**：`G:\周课表\_项目档案\交付与报告\周课表老师端问题根因审查报告.md`
- **升级规划报告**：`G:\周课表\_项目档案\交付与报告\问题反馈与升级规划报告.md`
- **git 仓库**：`https://github.com/Alphashen-fuck12/schedule-alpha-shen`（master）
*（内容由AI生成，仅供参考）*
