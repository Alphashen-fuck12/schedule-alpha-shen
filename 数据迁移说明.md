# 数据迁移说明（换电脑怎么保住课表）

> 课表数据保存在**本机的应用数据目录**里，而不是保存在 `index.html` / `schedule.exe` 文件里。
> 因此：**直接把 index.html 或 schedule.exe 复制到新电脑是带不走课表数据的**，必须使用页面自带的「导出 / 导入」功能。

---

## 〇、数据存在哪？

| 打开方式 | 数据存放位置 | 特点 |
| --- | --- | --- |
| **桌面应用 `schedule.exe`** | 该电脑**当前 Windows 用户**下的 WebView2 数据目录（应用自动创建的本地存储） | 独立保存，与浏览器数据互不相通 |
| **浏览器打开 `index.html`** | 浏览器的本地存储（localStorage） | 独立保存，与桌面应用数据互不相通 |

- **exe 版与 HTML 版数据不互通**：在桌面应用里填的课表，浏览器版里看不到，反之亦然。
- **换电脑 / 换 Windows 用户**：数据都不会自动跟着走，迁移必须通过「导出 JSON → 拷贝 → 导入 JSON」完成（见下方步骤）。
- 无论哪种方式，导出出的 `.json` 备份文件里已经包含全部课表数据（课程、设置、备忘录），导入后即可恢复，不受打开方式限制——**exe 版导出的备份，HTML 版也能导入**，反之亦然。

---

## 一、迁移步骤（三步走）

### 第 1 步：旧电脑上「导出备份」

1. 用旧电脑打开课表（桌面应用 `schedule.exe` 或浏览器打开 `index.html` 均可，数据以你平时使用的那份为准）。
2. 点击「课表设置」→ 在设置窗口下方找到 **「导出备份」** 按钮并点击。
3. 会下载一个 `.json` 备份文件（如 `课表备份_2026-08-30.json`），**这就是你的全部课表数据**（含课程、设置、备忘录）。

> 📷 截图占位：此处可放「导出备份」按钮位置截图。

### 第 2 步：把备份文件拷到新电脑

- 通过 U 盘、微信文件传输助手、QQ、网盘等任意方式，把刚才下载的 `.json` 文件复制到新电脑上。

### 第 3 步：新电脑上「导入备份」

1. 把课表带到新电脑：桌面应用版直接拷 `dist` 文件夹里的 `schedule.exe`（双击即用），浏览器版则拷 `index.html` 文件（可用 U 盘一起带过去）。
2. 用你喜欢的方式打开课表：**双击 `schedule.exe`**（推荐，Win10/11）或浏览器打开 `index.html`。
3. 点击「课表设置」→ 在设置窗口下方找到 **「导入备份」** 按钮并点击。
4. 选择刚才拷过来的 `.json` 备份文件。
5. 导入成功后课表恢复原样，即可正常使用。

> 📷 截图占位：此处可放「导入备份」文件选择截图。

---

## 二、常见问题

| 问题 | 说明 |
| --- | --- |
| 把 index.html 直接复制到新电脑，课表是空的？ | 正常现象。数据存在旧电脑浏览器里，不随文件走，必须用「导出 / 导入」迁移。 |
| 换浏览器（如 Chrome 换 Edge）打开旧电脑上的 index.html，课表也没了？ | 每个浏览器各自保存数据，请先在原浏览器里导出备份，再到新浏览器导入。 |
| 桌面应用版填的课表，浏览器版里看不到？ | 正常现象。exe 版与 HTML 版数据不互通，必须用「导出 / 导入」迁移，且导出的 .json 备份两种版本都能互相导入。 |
| 清理浏览器缓存 / 历史记录后课表不见了？ | 若清理了站点数据会删除本课表数据，**建议定期导出备份**妥善保存。 |
| 备份文件打不开 / 损坏？ | 平时多备份一份到不同位置（U 盘 + 网盘），双保险。 |

---

## 三、建议

- **定期备份**：每逢期中 / 期末或排课大调整后，导出一份备份并妥善保存（U 盘、网盘各一份）。
- **同一台电脑**：只要不清理浏览器站点数据，课表会一直自动保存在本机，无需重复导入。
