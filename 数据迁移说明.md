# 数据迁移说明（换电脑怎么保住课表）

> 课表数据保存在**浏览器的本地存储（localStorage）**里，而不是保存在 `index.html` 文件里。
> 因此：**直接把 index.html 复制到新电脑是带不走课表数据的**，必须使用页面自带的「导出 / 导入」功能。

---

## 一、迁移步骤（三步走）

### 第 1 步：旧电脑上「导出备份」

1. 用浏览器打开旧电脑上的 `index.html`。
2. 点击「课表设置」→ 在设置窗口下方找到 **「导出备份」** 按钮并点击。
3. 浏览器会下载一个 `.json` 备份文件（如 `课表备份_2026-08-30.json`），**这就是你的全部课表数据**（含课程、设置、备忘录）。

> 📷 截图占位：此处可放「导出备份」按钮位置截图。

### 第 2 步：把备份文件拷到新电脑

- 通过 U 盘、微信文件传输助手、QQ、网盘等任意方式，把刚才下载的 `.json` 文件复制到新电脑上。

### 第 3 步：新电脑上「导入备份」

1. 把 `index.html` 文件也复制到新电脑（可用 U 盘一起带过去，或在官网重新下载）。
2. 用浏览器打开新电脑上的 `index.html`。
3. 点击「课表设置」→ 在设置窗口下方找到 **「导入备份」** 按钮并点击。
4. 选择刚才拷过来的 `.json` 备份文件。
5. 导入成功后课表恢复原样，即可正常使用。

> 📷 截图占位：此处可放「导入备份」文件选择截图。

---

## 二、常见问题

| 问题 | 说明 |
| --- | --- |
| 把 index.html 直接复制到新电脑，课表是空的？ | 正常现象。数据存在旧电脑浏览器里，不随文件走，必须用「导出 / 导入」迁移。 |
| 换浏览器（如 Chrome 换 Edge）打开旧电脑上的 index.html，课表也没了？ | 每个浏览器各自保存数据，请先在原浏览器里导出备份，再到新浏览器导入。 |
| 清理浏览器缓存 / 历史记录后课表不见了？ | 若清理了站点数据会删除本课表数据，**建议定期导出备份**妥善保存。 |
| 备份文件打不开 / 损坏？ | 平时多备份一份到不同位置（U 盘 + 网盘），双保险。 |

---

## 三、建议

- **定期备份**：每逢期中 / 期末或排课大调整后，导出一份备份并妥善保存（U 盘、网盘各一份）。
- **同一台电脑**：只要不清理浏览器站点数据，课表会一直自动保存在本机，无需重复导入。
